{
    "name": "docktest",
    "description": "Simple test for Node-RED and Docker",
    "version": "0.0.1",
    "dependencies": {
        "node-red": "*"
    },
    "node-red": {
        "settings": {
            "flowFile": "flow.json",
            "credentialsFile": "flow_cred.json"
        }
    },
    "scripts": {
        "start": "node node_modules/node-red/red.js -u . flow.json"
    }
}